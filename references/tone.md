# Tone

A second declared asset, present only on the `v2` branch.

Its whole job is to be absent from `main` and present here, so that upgrading
changes the file tree and not only the text. A skill whose files did not move
with its content would show this file missing.
