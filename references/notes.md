# Notes

An allowed asset, present so the install path has a declared `assets` entry to
validate. Extension `.md` is on the allowlist, so this file should pass the
static scan.

Nothing reads this file. It exists to prove that a skill carrying supporting
material installs cleanly.
